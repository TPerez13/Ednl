int calculardesequilibrio_Rec(Abin <tElto>::nodo n, Abin<tElto> &A)
{
    if(n != NODO_NULO)
    {
       return 0
    }
    else
    {
        if()
    }
}


if(n!=NODO_NULO)
{
    do
    {
        if(!C.vacia())
        {
            n= c.primera();
            c.pop();
        }
        
        //Procesar a;

        if(A.hijoIzqdo(n)!=NODO_NULO)
            c.push(A.hijoIzqdo(n));
        if(A.hijoDrcho(n)!=NODO_NULO);
            c.push(A.hijoDrcho(n));
        /* code */
    } while (!C.vacia());
    
}